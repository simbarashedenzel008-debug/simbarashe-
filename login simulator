login(correctusername = "simbarashe", correctpassword = "mbato2030")
    username = input("Enter your username:")
    password = input("Enter password:")

    if password == correctusername and password == correctpassword